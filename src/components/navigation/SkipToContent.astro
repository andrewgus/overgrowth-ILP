---
import BaseButton from '../base/BaseButton.vue'
---

<div class='skipBtn' id='skipToContent'>
	<BaseButton
		link
		isWarning
		isDisabled={false}
		text='Skip to main content'
		url='#lessonHeading'
	/>
</div>

<style lang='scss'>
	.skipBtn {
		position: absolute;
		top: -10vh;
		max-width: max-content;
		border: 1px solid var(--white);
		transition: top var(--timeShort) ease-in-out;

		&:focus-within {
			top: 0;
		}
		> a {
			border-radius: 0 0 var(--s0) var(--s0);
			display: block;
			padding: var(--s-5);
			color: var(--white);
		}
	}
</style>
