---
---

<ol class='stepList'>
	<slot />
</ol>

<style lang='scss'>
	ol.stepList {
		counter-reset: stepListCounter;
		list-style-type: none;
		padding-left: 0;
		> :global(li) {
			counter-increment: stepListCounter;
			padding: var(--s0);
			&::before {
				content: 'Step ' counter(stepListCounter);
				font-weight: 700;
				font-size: var(--s1);
				display: block;
				width: max-content;
				padding: 0 var(--s-8);
				margin-left: calc(-1 * var(--s0));
			}
			+ :global(li) {
				margin-top: 0;
				border-top: 1px solid var(--darkGray);
			}
			&:nth-child(2n) {
				background-color: var(--offWhite);
			}
			> :global(dl) {
				margin-top: var(--s2);
			}
		}
	}
</style>
