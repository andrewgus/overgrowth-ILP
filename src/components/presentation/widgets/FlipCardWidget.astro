---
// NOTE: only use FlipCardItem component as a child (for the <slot/>) of this component.
type Props = {
	isFullWidth?: boolean
}
const { isFullWidth = false } = Astro.props
---

<div class:list={['flipCards', { fullWidth: isFullWidth }]}>
	<slot />
</div>

<style>
	.flipCards {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(20ch, 1fr));
		grid-gap: var(--s0);
		justify-items: stretch;
		align-items: stretch;
	}
</style>
